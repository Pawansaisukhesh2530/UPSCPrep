<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Internet permissions for Firebase -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Biometric authentication permissions -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <!-- Optional: Fingerprint hardware feature (not required) -->
    <uses-feature
        android:name="android.hardware.fingerprint"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.UPSCPrep">

        <!-- Splash Activity - Launcher (with auto-login) -->
        <activity
            android:name=".ui.auth.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.UPSCPrep"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Login Activity -->
        <activity
            android:name=".ui.auth.LoginActivity"
            android:exported="false"
            android:theme="@style/Theme.UPSCPrep" />

        <!-- Signup Activity -->
        <activity
            android:name=".ui.auth.SignupActivity"
            android:theme="@style/Theme.UPSCPrep" />

        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.UPSCPrep"
            android:exported="false" />

        <!-- Assignment/Quiz Activities -->
        <activity
            android:name=".ui.assignments.activities.SubjectSelectionActivity"
            android:exported="false"
            android:theme="@style/Theme.UPSCPrep" />

        <activity
            android:name=".ui.assignments.activities.GSPaperSelectionActivity"
            android:exported="false"
            android:theme="@style/Theme.UPSCPrep" />

        <activity
            android:name=".ui.assignments.activities.UnitSelectionActivity"
            android:exported="false"
            android:theme="@style/Theme.UPSCPrep" />

        <activity
            android:name=".ui.assignments.activities.QuizActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.UPSCPrep" />

        <activity
            android:name=".ui.assignments.activities.ResultActivity"
            android:exported="false"
            android:theme="@style/Theme.UPSCPrep" />

        <activity
            android:name=".ui.assignments.activities.SolutionReviewActivity"
            android:exported="false"
            android:theme="@style/Theme.UPSCPrep" />

        <activity
            android:name=".ui.assignments.activities.TestHistoryActivity"
            android:exported="false"
            android:theme="@style/Theme.UPSCPrep" />

    </application>

</manifest>